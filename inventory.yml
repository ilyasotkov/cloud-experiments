---
ungrouped:
  hosts:
    localhost:
      ansible_connection: local
swarm_nodes:
  hosts:
    leader_manager:
      ansible_host: INSERT_IP_ADDRESS_FROM_TERRAFORM_OUTPUT
      node_swarm_role: leader_manager
      node_hosting_provider: linode
  vars:
    ansible_user: root
    ansible_become: yes
  # children:
    # workers:
      # hosts:
      # vars:
    # managers:
      # hosts:
      # vars:
